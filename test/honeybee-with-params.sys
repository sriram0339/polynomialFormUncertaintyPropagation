var x init uniform(475,475)
var y1 init uniform(375, 375)
var y2 init uniform(125, 125)
var z1 init uniform(35, 35)
var z2 init uniform(35, 35)
var beta1 init uniform(0.0009, 0.0011)
var beta2 init uniform(0.0009, 0.0011)
var gamma init uniform(0.0, 0.5)
var delta init uniform(0.0, 1.0)
var alpha init uniform(0.0, 1.0)

x := x + 0.2 *(-beta1 * x * y1 - beta2 * x * y2)
y1 := y1 + 0.2 *( beta1 * x * y1 - gamma * y1 + delta * beta1 * y1 * z1 + alpha * beta1 * y1 * z2 )
y2 := y2 + 0.2 * (beta2 * x * y2 - gamma * y2 + delta * beta2 * y2 * z2 + alpha * beta2 * y2 * z1)
z1 := z1 + 0.2 * (gamma * y1 - delta * beta1 * y1 * z1 - alpha * beta2 * y2 * z1)
z2 := z2 + 0.2 * (gamma * y2 - delta * beta2 * y2 * z2 - alpha * beta1 * y1 * z2)
beta1 := beta1 
beta2 := beta2
gamma := gamma
delta := delta
alpha := alpha

query q0 observed x
end