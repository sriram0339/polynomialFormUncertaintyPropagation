var x1 init uniform(-1.0, -1.0)
var x2 init uniform(1.0, 1.0)
var alpha init uniform(0.2, 0.4)
var beta init uniform(0.1, 0.2)
var gamma init uniform(0.4, 0.6)

x1 := x1 + 0.05* gamma * (x1 - (x1^3)/3 + x2) 
x2 := x2 - 0.05 * ( x1 - alpha + beta * x2) / gamma
alpha := alpha
beta := beta
gamma := gamma

query q0 observed x1

end

