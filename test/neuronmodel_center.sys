var x1 init uniform(-1.0, -1.0)
var x2 init uniform(1.0, 1.0)
var alpha init uniform(-0.1, 0.1)
var beta init uniform(-0.05, 0.05)

x1 := x1 + 0.01 * (x1 - (x1^3)/3 + x2) 
x2 := x2 - 0.04 * ( x1 - (alpha+0.3) + (beta+0.15) * x2)
alpha := alpha
beta := beta

query q0 observed x1

end

